const Hotel = require("../models/HotelModel");


export const HotelController = {
    uploadInfo: async (req, res)=> {
          try{
             const 
          }catch(err){
             res.status.json(err);
          }
    }
};